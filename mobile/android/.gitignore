.gradle/
local.properties
*.iml
.idea/
build/
app/build/
field-logs/
# Built dashboard JS bundle + source maps. Editable source is in
# app/src/main/dashboard-src/js/; dashboard-tests/build.mjs (esbuild) compiles it
# into this dir, which Gradle's buildDashboardJs runs as part of preBuild.
app/src/main/assets/dashboard/js/
# Dependency-lock files are generated on-demand by the F1 OSV scan in CI (via
# --write-locks); never committed, so dependency locking imposes no version
# enforcement on builds. Covers app/gradle.lockfile + settings-gradle.lockfile.
*.lockfile
