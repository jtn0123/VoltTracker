{
  "name": "volttracker-dashboard-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "setup": "npm ci",
    "lint": "cd .. && eslint app/src/main/assets/dashboard/js/**/*.js",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^4.1.7",
    "eslint": "^10.4.0",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
