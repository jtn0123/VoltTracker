<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Partial wake lock while a real adapter session records, so Doze can't suspend the
         CPU mid-drive and stall Bluetooth polling. Released on session stop. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Remote map basemap tiles + the GitHub Releases update check/download;
         OBD/GPS data stays in app-private local storage. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- In-app updater: hands a downloaded release APK to the system installer.
         Android still shows its own confirmation sheet and refuses any APK not
         signed with the installed app's key. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Used by the troubleshooter's user-triggered force-stop button for known competing
         BT-OBD apps only. This is a normal-tier permission and is never used automatically. -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />

    <!-- On Android 11+ PackageManager only returns packages the caller can "see". We list every
         BT-OBD app the detector watches for so the competing-app UX works without the
         Play-Store-restricted QUERY_ALL_PACKAGES permission. Keep this list in sync with
         CompetingAppDetector.kt. -->
    <queries>
        <package android:name="io.tripovan.voltage" />
        <package android:name="com.gretio.obd" />
        <package android:name="org.prowl.torque" />
        <package android:name="org.prowl.torquefree" />
        <package android:name="org.prowl.torqueusb" />
        <package android:name="com.pnn.obdcardoctor_full" />
        <package android:name="com.pnn.obdcardoctor" />
        <package android:name="com.ovz.carscanner" />
        <package android:name="com.outils.obd2" />
    </queries>

    <application
        android:name=".VoltTrackerApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">
        <!-- Native Compose dashboard — the launcher experience. -->
        <activity
            android:name=".ComposeDashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.VoltTracker.Splash"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode|density|fontScale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Classic WebView dashboard. Still hosts trips/history/settings and the
             connection troubleshooter; reachable from the Compose Settings tab and
             the home-screen widget until the native screens absorb those flows. -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.VoltTracker.Splash"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode|density|fontScale">
        </activity>

        <service
            android:name="com.volttracker.obdpoc.service.ObdService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />

        <!-- Home-screen widget (M10a). exported="true" is required: the system AppWidget host is a
             separate process and delivers APPWIDGET_UPDATE to this receiver. The widget only reads a
             persisted snapshot and launches MainActivity, so it exposes no sensitive surface.
             android:permission="BIND_APPWIDGET" gates APPWIDGET_UPDATE to the system widget host so
             other apps cannot spam update broadcasts; our own in-app refresh uses a direct
             AppWidgetManager.updateAppWidget call (see WidgetUpdater.requestRefresh), not a
             broadcast, so the permission gate does not block it. -->
        <receiver
            android:name=".widget.VoltWidgetProvider"
            android:exported="true"
            android:permission="android.permission.BIND_APPWIDGET"
            android:label="@string/widget_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/volt_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
