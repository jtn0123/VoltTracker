<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Map basemap tiles only; all OBD/GPS data stays on-device. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Bucket 4a (C4) — used by MainActivity.forceStopPackage to terminate competing
         BT-OBD apps (Voltage, Torque, …) at the user's request from the troubleshooter. This
         is a normal-tier permission (no runtime grant) and only affects packages the user
         explicitly picks; we don't kill apps unprompted. -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />

    <!-- Bucket 2 (C4) — on Android 11+ getInstalledApplications only returns packages the
         caller can "see". We list every BT-OBD app the detector watches for by name so the
         "force-stop competing app" UX surfaces without us needing the Play-Store-restricted
         QUERY_ALL_PACKAGES permission. Keep this list in sync with CompetingAppDetector.java. -->
    <queries>
        <package android:name="io.tripovan.voltage" />
        <package android:name="com.gretio.obd" />
        <package android:name="org.prowl.torque" />
        <package android:name="org.prowl.torquefree" />
        <package android:name="org.prowl.torqueusb" />
        <package android:name="com.pnn.obdcardoctor_full" />
        <package android:name="com.pnn.obdcardoctor" />
        <package android:name="com.ovz.carscanner" />
        <package android:name="com.outils.obd2" />
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ObdService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
